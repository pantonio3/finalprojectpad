<!-- Validation failure messages -->
<% if @documenttype.errors.any? %>
  <% @documenttype.errors.full_messages.each do |message| %>
    <div class="alert alert-dismissable alert-danger">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <%= message %>
    </div>
  <% end %>
<% end %>

<div class="page-header">
  <h3>Edit Documenttype #<%= @documenttype.id %></h3>
</div>

<div class="row">
  <div class="col-md-12">
    <form action="/update_documenttype/<%= @documenttype.id %>" method="post">
      <!-- Hidden input for authenticity token to protect from forgery -->
      <input name="authenticity_token" type="hidden" value="<%= form_authenticity_token %>">

      <!-- Label and input for name -->
      <div class="form-group">
        <label for="name" class="control-label">
          Name
        </label>

        <input type="text" id="name" name="name" placeholder="Name" class="form-control" value="<%= @documenttype.name %>">
      </div>

      <button class="btn btn-warning">
        Update Documenttype
      </button>
      or
      <a href="/documenttypes/<%= @documenttype.id %>">Cancel</a>
    </form>
  </div>
</div>
